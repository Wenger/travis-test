{
    "name": "travis-test",
    "version": "0.0.1",
    "license": "MIT",
    "engines": {
        "node": ">= 7.0.0",
        "npm": ">= 6.0.0"
    },
    "scripts": {
        "preinstall": "echo $PATH && sudo -u ${SUDO_USER} echo $PATH && sudo -u ${SUDO_USER} cmake --vesion",
        "postinstall": "sudo -u ${SUDO_USER} cmake --vesion"
    },
    "os": [
        "darwin",
        "linux"
    ],
    "files": [
        "index.js"
    ]
}
